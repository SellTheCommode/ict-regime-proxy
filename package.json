{
  "name": "ict-regime-proxy",
  "version": "2.0.0",
  "description": "CORS proxy + WebSocket proxy for Tradovate API",
  "main": "proxy-server.js",
  "scripts": {
    "start": "node proxy-server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
